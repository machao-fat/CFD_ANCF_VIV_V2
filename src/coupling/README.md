# Coupling source

This tree contains the generic coordinator, preCICE backends, mapping helpers,
checkpoint utilities and the HH06 Structure_0000 wrapper extracted from the HH06
development lineage.

The wrapper and adapter paths are qualification/development code unless the validation
ledger explicitly marks a path as production-qualified. The current single-slice
bounded FSI is not passed.
